# Environment files — never commit real secrets
.env
.env.*
!.env.example

# Belt-and-suspenders: explicit paths for the most sensitive files
server/.env
server/.env.local
web/.env.local

# Auth cookies
**/cookies.txt

# Legacy bare names (kept for safety)
env
env.example

# Node
node_modules/
**/.cache/

# Build outputs
dist/
.next/
out/

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Logs
*.log

# Database backups — never commit production dumps
*.dump
*.backup
backups/

# Claude folder
.claude/
.dream-massage-seed-data/