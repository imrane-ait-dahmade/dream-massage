// Background job: polls Shelly Cloud every SYNC_INTERVAL_MS milliseconds.
// Fetches all 5 chairs in ONE HTTP request, feeds readings into the
// chair state machine, and broadcasts updates via Socket.IO.
// TODO: implement in Session 1

export {};
