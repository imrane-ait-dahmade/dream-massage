// Calculates the expected amount for a completed session.
// Applies the active PricingRule (grace window, rounding mode, overtime policy)
// against all active PricingPlans and writes expected_amount + pricing_snapshot.
// TODO: implement in Session 1

export {};
